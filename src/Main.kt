fun main() {
var favouriteActor:String? = null         // by putting the ?, it makes it nullable
    favouriteActor = "Meryl Sreep"
    println(favouriteActor)

    var number:Int?  = null
    println(number)
}